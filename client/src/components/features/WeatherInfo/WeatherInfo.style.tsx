import styled from "styled-components";

const WeatherCardConditionDiv = styled.div`
  & p {
    text-transform: uppercase;
  }
`;

const Styled = {
  WeatherCardConditionDiv,
};

export default Styled;
